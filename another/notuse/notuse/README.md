This folder contains legacy per-language handlers that have been superseded by the unified category handler at GET /category/{slug}.

All files here are excluded from normal builds via `//go:build notuse` build tags. Keep them for reference or delete as needed.
